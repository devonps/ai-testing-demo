# Copy to .env and fill in locally. Never commit .env.
# Which environment config (config/<TEST_ENV>.json) to load.
TEST_ENV=dev

# No secrets are required yet. When credentials/API keys are needed,
# add them here as KEY=value and read them via process.env — never commit real values.
